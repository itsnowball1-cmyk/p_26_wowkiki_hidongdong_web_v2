# NAS 배포 환경변수. 실제 값은 .env 로 복사 후 채워서 사용.
# .env 는 git 에 커밋되지 않음.

# DB root 비밀번호 (16자 이상 권장)
DB_PASSWORD=change-me-strong-password

# DB 이름
DB_DATABASE=wowkiki

# 미디어/녹음 파일 베이스 URL (있다면)
FILES_BASE_URL=
