# NAS 개발(dev) 배포 환경변수. 실제 값은 .env 로 복사 후 채워서 사용.
# .env 는 git 에 커밋되지 않음.

# DB root 비밀번호 (운영과 다르게 — 별도 강력 비번)
DB_PASSWORD=change-me-strong-dev-password

# DB 이름
DB_DATABASE=wowkiki

# 음성파일 베이스 URL (운영 동일)
FILES_BASE_URL=https://api.wowkiki.kr
